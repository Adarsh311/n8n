{
  "name": "n8n-on-render",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "n8n start",
    "build:custom-nodes": "for dir in custom-nodes/n8n-nodes-*/; do if [ -d \"$dir\" ] && [ -f \"$dir/package.json\" ]; then echo \"Building $dir\" && (cd \"$dir\" && npm run build); fi; done"
  },
  "dependencies": {
    "n8n": "2.8.3"
  },
  "n8n": {
    "customExtensionsPath": "custom-nodes/"
  }
}
